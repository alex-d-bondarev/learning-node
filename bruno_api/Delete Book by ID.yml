info:
  name: Delete Book by ID
  type: http
  seq: 5

http:
  method: DELETE
  url: http://{{app_host}}:3000/books/{{book_id}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
