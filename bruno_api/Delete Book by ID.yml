info:
  name: Delete Book by ID
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{app_url}}/{{book_endpoint}}/{{book_id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
