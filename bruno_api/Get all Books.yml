info:
  name: Get all Books
  type: http
  seq: 2

http:
  method: GET
  url: "{{app_url}}/{{book_endpoint}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
