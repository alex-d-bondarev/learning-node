info:
  name: Delete Book by ID
  type: http
  seq: 1

http:
  method: DELETE
  url: "{{app_url}}/{{book_endpoint}}/{{book_id}}"
  headers:
    - name: Accept-Language
      value: "{{locale}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
