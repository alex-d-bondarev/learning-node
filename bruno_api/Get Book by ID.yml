info:
  name: Get Book by ID
  type: http
  seq: 3

http:
  method: GET
  url: "{{app_url}}/{{book_endpoint}}/{{book_id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
